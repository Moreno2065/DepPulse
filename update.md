# DepPulse 改进方案

> 当前状态：能扫描依赖图、分析变更影响面、输出报告，但分析结果停留在终端，没有嵌入实际工作流。

## 三个场景

| # | 场景 | 痛点 | 目标 |
|---|------|------|------|
| A | CI 测试筛选 | 改两行跑全量测试，浪费 CI 时间 | 自动算出该跑哪些测试喂给 pytest |
| B | 架构趋势监控 | 依赖腐化是渐进的，没人持续盯着 | 每次 commit 存快照，指标暴涨时报警 |
| C | PR 影响面评论 | reviewer 只看 diff，不看全局影响 | 自动在 PR 里评论改动影响了哪些模块 |

---

## 场景 A：CI 测试筛选

### 流程

```
git diff → changed files → 扫描项目（含 tests/）→ ImpactAnalyzer 追溯上游
→ 过滤出测试文件 → 输出给 pytest
```

### CLI

```bash
deppulse tests --since main              # 对比分支
deppulse tests --since HEAD~1            # 对比 ref
deppulse tests --files a.py b.py         # 显式指定文件
deppulse tests --since main --format args   # pytest $(...) 消费
deppulse tests --since main --format json   # CI 解析
deppulse tests --since main --max-blast 50  # 影响面太大时 fallback 输出全部
```

### 测试文件识别（三层策略，按优先级）

1. **文件名模式**：`test_*.py`、`*_test.py`、`*Test.java`、`*Spec.kt`
2. **路径模式**：在 `tests/`、`test/`、`spec/`、`src/test/` 下
3. **约定映射兜底**：changed file 不在图中时（如配置文件），`src/foo.py` → `tests/test_foo.py`

### 输出格式

**list（默认）：**
```
tests/test_analyzer.py
tests/test_risk.py
```

**args：**
```
tests/test_analyzer.py tests/test_risk.py
```

**json：**
```json
{
  "changed_files": ["deppulse/core/analyzer.py"],
  "selected_tests": ["tests/test_analyzer.py", "tests/test_risk.py"],
  "by_strategy": {
    "graph": ["tests/test_analyzer.py", "tests/test_risk.py"],
    "convention": []
  }
}
```

### CI 集成

```yaml
- name: Select affected tests
  id: tests
  run: |
    SELECTED=$(deppulse tests --since origin/main --format args)
    echo "tests=$SELECTED" >> $GITHUB_OUTPUT

- name: Run affected tests
  if: steps.tests.outputs.tests != ''
  run: pytest ${{ steps.tests.outputs.tests }} -v
```

### 涉及改动

| 文件 | 改动 |
|------|------|
| `deppulse/cli.py` | 新增 `tests` 子命令 + handler |
| `deppulse/config.py` | 新增 `test_patterns`、`test_dirs` 配置 |
| `deppulse/core/test_selector.py` | **新文件**：依赖图追溯 + 三层过滤 + 约定映射 |
| `deppulse/models.py` | 新增 `TestSelectionResult` dataclass |
| `deppulse/ui/render.py` | 新增 `render_test_selection()` |

---

## 场景 B：架构趋势监控

### 流程

```
每次 commit / 定期触发 → 扫描建图 → 计算指标 → 与历史基线对比 → 超阈值报警
```

### CLI

```bash
deppulse snapshot save --tag "v0.2.0"            # 保存快照
deppulse snapshot diff --from v0.1.0 --to v0.2.0 # 对比两个快照
deppulse snapshot list                            # 列出所有快照
deppulse snapshot check --since-tag HEAD~10       # CI 模式：自动对比报警
```

### 监控指标及默认阈值

| 指标 | 含义 | 报警阈值 |
|------|------|---------|
| in-degree 变化率 | 某文件的依赖方数量增长 | >100% |
| 新增循环依赖 | 引入之前不存在的环 | >0 |
| 核心文件变更 | core/lib 等关键目录被改动 | 任何改动 |
| 总边数增长 | 项目整体耦合度 | >30% |
| unresolved 依赖变化 | 新引入无法解析的导入 | >5 |

### 快照存储

```
.deppulse/snapshots/
  2026-05-20-v0.1.0.json
  2026-05-22-def5678.json
  snapshot-index.json
```

每个快照 = 完整 `GraphBuildResult` + 每个文件的 in-degree/out-degree/betweenness + 环列表。

### 涉及改动

| 文件 | 改动 |
|------|------|
| `deppulse/cli.py` | 新增 `snapshot` 子命令（save/diff/list/check） |
| `deppulse/core/snapshot.py` | **新文件**：快照保存/加载/对比/趋势计算 |
| `deppulse/models.py` | 新增 `SnapshotMeta`、`SnapshotDiff`、`TrendAlert` |
| `deppulse/ui/render.py` | 快照对比终端表格 |

---

## 场景 C：PR 影响面评论

### 流程

```
PR 创建/更新 → GitHub Actions 触发 deppulse diff → 生成报告 → 贴到 PR comment
```

### CLI

```bash
deppulse pr-report --base main --format github-comment  # PR comment 格式
deppulse pr-report --base main --format markdown         # 纯 markdown
deppulse pr-report --base main --fail-on-high-risk      # 高风险直接 CI fail
```

### PR Comment 示例

````markdown
## DepPulse Impact Report

**Blast radius:** 12 files affected (15% of project)
**Risk level:** HIGH (score 72)

| File | In-degree | Risk |
|------|-----------|------|
| `core/analyzer.py` | 8 | HIGH |
| `models.py` | 15 | MEDIUM |

### Suggested tests
- `tests/test_analyzer.py`
- `tests/test_orchestrator.py`

> Generated by DepPulse
````

### CI 集成

```yaml
- name: Post impact report
  uses: actions/github-script@v7
  with:
    script: |
      const report = execSync(
        'deppulse pr-report --base ${{ github.base_ref }} --format markdown'
      ).toString();
      github.rest.issues.createComment({
        ...context.repo,
        issue_number: context.issue.number,
        body: report
      });
```

### 涉及改动

| 文件 | 改动 |
|------|------|
| `deppulse/cli.py` | 新增 `pr-report` 子命令 |
| `deppulse/core/pr_reporter.py` | **新文件**：PR 报告生成（markdown 模板 + 数据聚合） |
| `deppulse/models.py` | 新增 `PRReportResult` dataclass |
| `deppulse/ui/render.py` | 新增 `render_pr_report()` |

---

## 实现优先级

```
1. 场景 A ← 最实用，直接省 CI 时间
2. 场景 C ← 不改核心逻辑，主要是模板 + CI 集成
3. 场景 B ← 需要持久化存储，体量最大
```

## 公共基础（已存在，不需要重写）

- `ImpactAnalyzer` — 追溯上游影响面
- `DependencyOrchestrator` — 扫描 + 建图
- `git.py` — 获取变更文件
- `risk.py` — 风险评分

## 注意事项

- 场景 A：确认 `tests/` 不在默认 ignore_dirs 中（当前已确认 OK）
- 场景 B：快照文件加入 `.gitignore`（`.deppulse/snapshots/`）
- 场景 C：markdown 模板要稳定，别频繁改格式
- 所有场景：处理好"没有变更"和"变更文件不在图中"的边缘情况
